<Window x:Class="GitContextMenu.UI.Views.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:GitContextMenu.UI.ViewModels"
        xmlns:views="clr-namespace:GitContextMenu.UI.Views"
        Width="700" MinHeight="460" MaxHeight="820"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="SingleBorderWindow"
        Background="{StaticResource BrushBg}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:OperationViewModel}">
            <views:OperationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StatusViewModel}">
            <views:StatusView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CommitViewModel}">
            <views:CommitView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:BranchViewModel}">
            <views:BranchView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── Header ─────────────────────────────────────────────── -->
        <Border Grid.Row="0" Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="0,0,0,1">
            <Grid Margin="14,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Ikona gałęzi + tytuł operacji -->
                <StackPanel Grid.Column="0" Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Border Width="22" Height="22" CornerRadius="4"
                            Background="{StaticResource BrushAccent}"
                            Margin="0,0,10,0">
                        <TextBlock Text="⎇" FontSize="11" FontWeight="Bold"
                                   Foreground="{StaticResource BrushAccentFg}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="{Binding Title}"
                               FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource BrushText}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Ścieżka repozytorium — skrócona -->
                <TextBlock Grid.Column="2"
                           Text="{Binding RepoPath}"
                           FontSize="11" FontFamily="Consolas, Courier New"
                           Foreground="{StaticResource BrushMuted}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           MaxWidth="360"/>
            </Grid>
        </Border>

        <!-- ── Treść (swappable przez DataTemplates) ──────────────── -->
        <ContentControl Grid.Row="1" Content="{Binding}"
                        Background="{StaticResource BrushBg}"/>

        <!-- ── Status bar ─────────────────────────────────────────── -->
        <Border Grid.Row="2"
                Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="0,1,0,0"
                Padding="14,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{Binding StatusBrush}"
                           FontSize="12"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
                <ProgressBar Grid.Column="1"
                             IsIndeterminate="True"
                             Width="60" Height="3"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
